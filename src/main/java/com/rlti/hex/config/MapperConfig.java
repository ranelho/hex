package com.rlti.hex.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MapperConfig {
    // MapStruct não precisa de configuração adicional aqui, pois é configurado via anotações
    // e processamento em tempo de compilação
}
